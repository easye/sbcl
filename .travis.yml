language: c

os:
  - osx

install:
  - bash ci/install-sbcl.bash
  
script:
  - which git
# Fake a version  
#  - echo '"1.0.99.999"' > version.lisp-expr
  - sh echo $PATH
  - sh ./make.sh
  - cd tests && sh run-tests.sh

  
