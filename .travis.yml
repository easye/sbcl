language: c

os:
  - osx

install:
  - bash ci/install-sbcl.bash
  
script:
# make.sh can't seem to find git in its path, so we get medieval 
  - ln $(which git) /usr/bin/git
  - sh ./make.sh
  - cd tests && sh run-tests.sh

  
